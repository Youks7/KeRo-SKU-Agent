# Single-Agent Forward Run Record

These records apply each of the eight marketplace skills to the same sanitized sunglasses `SKU_CONTEXT`.

These historical runs verify workflow logic, evidence gating, product-fidelity inheritance, platform-specific outputs, and representative final prompts from the initial V1.3 split. They predate the later full per-unit protocol and usability cleanup. They do not prove the current one-entry flow, Mode A/Mode B contract behavior, or clean-session implicit triggering because the local Codex CLI could not authenticate through the restricted test environment.

Expected files: `taobao.md`, `tmall.md`, `pinduoduo.md`, `jd.md`, `1688.md`, `amazon.md`, `shopify.md`, and `tiktok-shop.md`.

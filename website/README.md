# Website

This directory contains a minimal static website for KeRo SKU Skill.

It can be deployed to:

- GitHub Pages
- Cloudflare Pages
- Netlify
- Vercel static hosting

No backend, database, or build step is required.

Open `index.html` directly or deploy the `website/` directory as a static site.

# 生图模型路由

生成 Prompt 前确认目标工具及其能力：是否支持参考图编辑、局部编辑、透明背景、负面提示词、固定画幅和高分辨率输出。

- 支持参考图编辑：明确唯一产品参考、允许修改区和禁止修改区。
- 只支持文字生图：真实销售 SKU 默认只生成背景，产品后期合成。
- 不支持 Negative Prompt：把关键限制写入正向指令和后续质检，不伪造无效参数。
- 模型未知：输出模型无关的画面规范，再附通用英文 Prompt，不添加专有参数。

Prompt 只负责视觉资产。最终文案、Logo、参数、认证和 CTA 使用独立排版或网页组件。

